<?php if (!defined('SLZ')) die('Forbidden');
$ext = slz_ext('shortcodes');
if (! is_admin()) {
	// wp_enqueue_style(
	// 		'transera-slz-extension-shortcodes-counter',
	// 		$ext->locate_URI('/static/css/transera-counter.css'),
	// 		array(),
	// 		$ext->manifest->get('version')
	// );
	// wp_enqueue_style(
	// 		'transera-slz-extension-shortcodes-map',
	// 		$ext->locate_URI('/static/css/transera-map.css'),
	// 		array(),
	// 		$ext->manifest->get('version')
	// );
	// wp_enqueue_style(
	// 		'transera-slz-extension-shortcodes-main-title',
	// 		$ext->locate_URI('/static/css/transera-main-title.css'),
	// 		array(),
	// 		$ext->manifest->get('version')
	// );
	// wp_enqueue_style(
	// 		'transera-slz-extension-shortcodes-service-list',
	// 		$ext->locate_URI('/static/css/transera-service-list.css'),
	// 		array(),
	// 		$ext->manifest->get('version')
	// );
	// wp_enqueue_style(
	// 		'transera-slz-extension-shortcodes-teams',
	// 		$ext->locate_URI('/static/css/transera-teams.css'),
	// 		array(),
	// 		$ext->manifest->get('version')
	// );
	// wp_enqueue_style(
	// 		'transera-slz-extension-shortcodes-tabs',
	// 		$ext->locate_URI('/static/css/transera-tabs.css'),
	// 		array(),
	// 		$ext->manifest->get('version')
	// );
}


if (is_admin()) {

}